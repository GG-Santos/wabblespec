# Project Map

**Project:** ___ UNDECLARED  
**Platform:** API-Service  
**Language:** ___ UNDECLARED (Node.js / Python / Go / Java / Rust)  
**Scaffolded:** ___ UNDECLARED  
**explored_at:** ___ UNDECLARED  
**build_target:** api-service  
**freshness_state:** FRESH  
**valid_until:** ___ UNDECLARED (24h from explored_at)  
**WabbleSpec version:** ___ UNDECLARED  

---

## Entry Points

_Fill in the language-appropriate entry point:_

- **Node.js:** `src/index.ts` or `src/server.ts`
- **Python:** `src/main.py` or `app/main.py` (FastAPI/Flask)
- **Go:** `cmd/{service-name}/main.go`
- **Java:** `src/main/java/{package}/Application.java`
- **Rust:** `src/main.rs`

_Update to match actual entry after Scaffold generates structure._

## Tech Stack

| Area | Technology | Version | Confidence |
|---|---|---|---|
| runtime | ___ UNDECLARED (Node/Python/Go/JVM/Rust) | ___ UNDECLARED | exact |
| framework | ___ UNDECLARED (Express/Fastify/FastAPI/Gin/Axum) | ___ UNDECLARED | exact |
| language | ___ UNDECLARED | ___ UNDECLARED | exact |
| database | ___ UNDECLARED | ___ UNDECLARED | inferred |
| orm | ___ UNDECLARED | ___ UNDECLARED | inferred |
| test | ___ UNDECLARED (Jest/pytest/go test/JUnit) | ___ UNDECLARED | exact |
| container | Docker | ___ UNDECLARED | inferred |
| lint | ___ UNDECLARED | ___ UNDECLARED | exact |
| ci | ___ UNDECLARED | n/a | inferred |
| observability | ___ UNDECLARED (Prometheus/Datadog/CloudWatch) | n/a | assumed |

_Confidence: `exact` = read from lock file, `inferred` = read from manifest without lock, `assumed` = no manifest evidence._

## Risk Files

| Path | Risk reason | Churn count | Note |
|---|---|---|---|
| `src/routes/` (or equivalent) | cross-cutting | 0 | Route definitions — all endpoint changes pass through here |
| `src/middleware/` (or equivalent) | cross-cutting | 0 | Auth, validation, error handling — affects all requests |
| `src/db/` (or equivalent) | no-tests | 0 | Database layer — integration tests needed, not unit tests |
| `Dockerfile` | cross-cutting | 0 | Container config change affects all deployments |

_Populate from git history after first N commits._

## Impact Slices

### entry-points
**Reason:** Server startup and route registration — structural changes start here.

- `src/index.ts` (update to actual entry)
- `src/routes/` (update to actual route directory)
- `src/app.ts` (or equivalent app factory — update)

### api-surface
**Reason:** Public API — routes, request/response schemas, OpenAPI spec.

- `src/routes/` (all route files)
- `src/schemas/` (request/response validation schemas — update)
- `openapi.yaml` (or `swagger.json` — update if present)

**Test paths:**
- `tests/routes/` or `src/routes/__tests__/` (update to actual convention)
- `tests/integration/` (integration test directory)

### test-coverage
**Reason:** Unit + integration test locations — identifies uncovered routes and handlers.

- `tests/` (root test directory — update)
- `src/**/*.test.ts` (colocated unit tests — update)
- `tests/integration/` (integration tests against running service)

### risk
**Reason:** Files from Risk Files table above.

- `src/routes/`
- `src/middleware/`
- `src/db/`
- `Dockerfile`

### conventions
**Reason:** Handler, schema, and naming patterns for this service.

- _See Conventions section below_

## Spec Artifacts

| Path | Artifact type | Status |
|---|---|---|
| `.wabblespec/receipts/scaffold-receipt-{timestamp}.json` | scaffold-receipt | LOCKED |

## Conventions

- Route handlers: one file per resource (e.g. `users.routes.ts`, `orders.routes.ts`)
- Request validation: schema-first (Zod / Pydantic / struct tags — confirm after first wave)
- Error responses: structured JSON with `error_code` and `message` fields (per error-event-catalog.md)
- HTTP method conventions: GET (read), POST (create), PUT (replace), PATCH (update), DELETE (remove)
- ___ UNDECLARED — add project-specific conventions after first Explore run

## Git State

- **Active branch:** ___ UNDECLARED
- **Recent changes:** (none — fresh scaffold)
- **Uncommitted count:** ___ UNDECLARED

## Gaps

- Database schema not yet defined (unknown until migration files are written)
- Authentication mechanism not confirmed (JWT / API key / OAuth — unknown until auth middleware exists)
- Rate limiting strategy not determined
- OpenAPI spec: absent on fresh scaffold — update after first route is implemented
- Deployment target not confirmed (Kubernetes / ECS / Lambda / bare metal — see build-toolchain.md)
- Observability stack not confirmed (assumed — verify after first deploy)
- Test coverage baseline: 0% (fresh scaffold)

---

*Generated by Scaffold from `_shared/templates/scaffold/project-map-api-service.md`. Replace all `___ UNDECLARED` with actual values. Explore updates this file after generation.*
