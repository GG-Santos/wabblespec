# Project Map

**Project:** ___ UNDECLARED  
**Platform:** Web  
**Language:** TypeScript  
**Scaffolded:** ___ UNDECLARED  
**explored_at:** ___ UNDECLARED  
**build_target:** web  
**freshness_state:** FRESH  
**valid_until:** ___ UNDECLARED (24h from explored_at)  
**WabbleSpec version:** ___ UNDECLARED  

---

## Entry Points

- `src/main.tsx` (or `src/index.tsx` / `app/page.tsx` — update to match actual entry)
- `src/App.tsx`
- `vite.config.ts` (or `next.config.js` / `webpack.config.js`)

## Tech Stack

| Area | Technology | Version | Confidence |
|---|---|---|---|
| runtime | Node.js | ___ UNDECLARED | exact |
| framework | React (or Vue / Svelte — update) | ___ UNDECLARED | exact |
| language | TypeScript | ___ UNDECLARED | exact |
| bundler | Vite (or Next.js / webpack — update) | ___ UNDECLARED | exact |
| test | Vitest (or Jest — update) | ___ UNDECLARED | exact |
| e2e | Playwright (or Cypress — update) | ___ UNDECLARED | inferred |
| css | ___ UNDECLARED | ___ UNDECLARED | inferred |
| state | ___ UNDECLARED | ___ UNDECLARED | inferred |
| lint | ESLint + Prettier | ___ UNDECLARED | exact |
| ci | ___ UNDECLARED | n/a | inferred |

_Confidence: `exact` = read from lock file, `inferred` = read from manifest without lock, `assumed` = no manifest evidence._

## Risk Files

| Path | Risk reason | Churn count | Note |
|---|---|---|---|
| `src/App.tsx` | cross-cutting | 0 (fresh scaffold) | Root component — imported by all routes |
| `vite.config.ts` | cross-cutting | 0 | Build config change affects all output |
| `src/api/` | no-tests | 0 | API layer — test coverage must be added |

_Populated by Explore from git history. On fresh scaffold, churn count = 0; update after first N commits._

## Impact Slices

### entry-points
**Reason:** Files that are always affected by structural changes.

- `src/main.tsx`
- `src/App.tsx`
- `index.html`

### api-surface
**Reason:** Public API boundaries — internal components consumed by other modules or externally.

- `src/api/` (API client layer — update to actual paths)
- `src/hooks/` (custom hooks exported for reuse — update)
- `src/types/` (shared TypeScript types — update)

**Test paths:**
- `src/api/__tests__/` (update to actual test path convention)

### test-coverage
**Reason:** Where test files live — identifies uncovered areas.

- `src/**/__tests__/` (Vitest colocated tests)
- `e2e/` (Playwright tests — update)
- `src/**/*.test.ts`

### risk
**Reason:** Files from the Risk Files table above.

- `src/App.tsx`
- `vite.config.ts`
- `src/api/`

### conventions
**Reason:** Observed naming and structure patterns for this project.

- _See Conventions section below_

## Spec Artifacts

| Path | Artifact type | Status |
|---|---|---|
| `.wabblespec/receipts/scaffold-receipt-{timestamp}.json` | scaffold-receipt | LOCKED |

_Populated by Explore. On fresh scaffold: only scaffold receipt present._

## Conventions

- Component files: PascalCase (e.g. `UserProfile.tsx`)
- Hook files: camelCase prefixed `use` (e.g. `useAuthState.ts`)
- Test files: colocated `__tests__/` or `.test.ts` suffix
- API files: `src/api/{resource}.ts` pattern
- Types: `src/types/{domain}.ts` pattern
- ___ UNDECLARED — add project-specific conventions after first Explore run

## Git State

- **Active branch:** ___ UNDECLARED
- **Recent changes:** (none — fresh scaffold)
- **Uncommitted count:** ___ UNDECLARED

## Gaps

- Exact routing framework and route structure not yet determined (add after first feature wave)
- State management pattern not confirmed (local state / Context / Zustand / Redux — unknown until code exists)
- Authentication pattern not determined
- Backend API base URL and auth scheme unknown
- Test coverage baseline: 0% (fresh scaffold — no tests yet)
- CI/CD pipeline not confirmed (inferred from build-toolchain.md — verify after first deploy)

---

*Generated by Scaffold from `_shared/templates/scaffold/project-map-web.md`. Replace all `___ UNDECLARED` with actual values. Explore updates this file after generation — do not hand-edit the Tech Stack or Entry Points sections after Explore has run.*
