# Task Card

**goal:** generate-slide.py and slide-token-validator.py exist in .wabblespec/engine/shared/scripts/ producing token-safe, self-contained Chart.js HTML presentations using embedded design tokens, and present/SKILL.md documents the HTML output path in both engine and skills locations.
**target:** Framework
**complexity:** Medium
**change_class:** ADDITIVE
**locked_at:** 2026-05-31T09:48:00Z
**session_id:** html-slides-20260531

## Non-Goals

- Runtime calls to core.py or design_system.py — token data is embedded
- Gemini API / AI logo + CIP generation (separate Tier 7)
- Modifying colors.csv, charts.csv, or any reference directory file
- Daemon/stop-hook wiring for generate-slide.py
- PowerPoint (.pptx) output path — unchanged
- New module registration in wabblespec.yaml

## Assumptions

- Python 3 available; no third-party pip packages required beyond standard library
- Chart.js loaded from CDN in generated HTML — no local install
- Color palettes embedded from colors.csv as Python dict (161 entries) — no runtime CSV read
- Chart type mappings embedded from charts.csv — no runtime CSV read
- present SKILL.md update is strictly additive; existing .pptx workflow unchanged
- I11: both scripts go to .wabblespec/engine/shared/scripts/ (framework space only)

## Acceptance Criteria

### AC1 — generate-slide.py produces token-complete HTML

Given generate-slide.py exists in .wabblespec/engine/shared/scripts/
When it is run with a product query and content spec
Then the output HTML contains a Chart.js CDN script tag and CSS variables for all 16 token roles (--color-primary through --color-ring) plus spacing tokens (--space-xs through --space-3xl) and shadow tokens (--shadow-sm through --shadow-xl)

### AC2 — palette lookup resolves known product type

Given the embedded palette dict contains 161 product-type entries
When a query matching a known entry (e.g. Analytics Dashboard) is provided
Then the generated HTML CSS variables reflect that palette's primary, secondary, accent, background, and foreground hex values

### AC3 — slide-token-validator.py passes clean output

Given a valid HTML slide is generated by generate-slide.py
When slide-token-validator.py is run against the output file
Then it exits 0 and reports 0 violations

### AC4 — slide-token-validator.py catches raw values

Given an HTML file contains a raw hex value (e.g. color: #2563EB) or rgb() in a style attribute or inline style block
When slide-token-validator.py is run against that file
Then it exits non-zero and reports the violation with the line number

### AC5 — chart type mapping renders correct Chart.js type

Given the embedded chart-type mapping covers at least line, bar, pie, scatter, and area types
When generate-slide.py generates a slide specifying one of those types
Then the output HTML contains a Chart.js new Chart() call with the correct type field

### AC6 — unknown product query falls back gracefully

Given generate-slide.py is run with a product query matching none of the 161 embedded entries
When the script runs
Then it falls back to the SaaS (General) palette and exits 0 without crashing

### AC7 — present engine SKILL.md updated

Given the task is complete
When .wabblespec/engine/modules/l7/present/SKILL.md is read
Then it contains an HTML Slide Output section documenting generate-slide.py usage, token compliance requirement, and CDN dependency

### AC8 — present skills SKILL.md mirrored

Given the engine module SKILL.md has been updated
When .claude/skills/present/SKILL.md is read
Then it contains the identical HTML Slide Output section as the engine module version
