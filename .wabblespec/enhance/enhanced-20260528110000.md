# Enhanced Input — toprank-integration-phase2-T2

**Original message:** "proceed"
**Enhanced at:** 2026-05-28T11:00:00Z
**Recipe target:** Library-Package | Complexity: Medium

---

## Task

Implement T2 from the toprank-main integration plan Phase 2: create `llm-eval.py`, a WabbleSpec-native skill-section quality scorer, as a framework authoring task.

## Artifacts In Scope

### New script: `.wabblespec/engine/shared/scripts/llm-eval.py`
- Accepts `--skill-path <path>` and `--section <heading>` to extract a named section from a SKILL.md, OR `--content-file <path>` for raw text input
- Scores on three dimensions: clarity (can an agent understand each step?), completeness (are commands and expected behavior documented?), actionability (can an agent execute without follow-up questions?)
- Each dimension scored 1–5; minimum gate: 4/5 on all three
- Judge call routes through `runtime-state.json` `analysis` capability descriptor — NO hardcoded model name anywhere in the script (I6)
- Outputs JSON: `{clarity: N, completeness: N, actionability: N, reasoning: "...", passed: bool}`
- Appends one entry to `.wabblespec/state/evals/eval-log.json` (creates file/dir if absent)
- Exits 0 if all three dimensions ≥ 4; exits 1 with a formatted report if any dimension < 4

### New directory + file: `.wabblespec/state/evals/eval-log.json`
- Append-only JSON array of eval entries
- Each entry: `{timestamp, skill_path, section, scores: {clarity, completeness, actionability}, reasoning, passed}`
- Created by the script on first run if absent

### Benchmark SKILL.md update: `.claude/skills/benchmark/SKILL.md`
- Add routing entry to `## Reference Routing` section (or create one if absent) pointing `llm-eval.py` usage → `.wabblespec/engine/shared/scripts/llm-eval.py`
- This is a SKILL.md content edit, not a new reference file

## Constraints

- No hardcoded model name in `llm-eval.py` — I6 violation if present
- Read capability from `runtime-state.json` at `.wabblespec/state/runtime/runtime-state.json`; if absent or `analysis` capability missing, exit with a clear error message (do not fallback to a named model)
- `llm-eval.py` writes only to `.wabblespec/state/evals/` — no other framework space writes
- Benchmark SKILL.md line count may increase slightly (adding routing entry) — acceptable since this is an additive reference, not a bloating edit
- Do not copy `llm_judge.py` call_judge function; write a clean WabbleSpec-native API call using the capability descriptor pattern

## Open Assumptions

- `runtime-state.json` exists at `.wabblespec/state/runtime/runtime-state.json` with an `analysis` capability entry (confirmed: runtime-probe was run previously)
- The `analysis` capability entry in `runtime-state.json` contains enough information to construct an API call (model ID or endpoint)
- Benchmark SKILL.md does not already have a `## Reference Routing` section (confirmed from prior session reading)
