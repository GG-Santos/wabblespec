{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Clean Receipt",
  "description": "Extension fields for clean-receipt.json in .wabblespec/receipts/",
  "type": "object",
  "required": ["scope", "files_analyzed", "cosmetic_changes", "additive_changes", "breaking_halted", "diff_path"],
  "properties": {
    "scope": {
      "type": "string",
      "description": "Declared scope — file path, directory, pattern, or named scope"
    },
    "files_analyzed": {
      "type": "integer",
      "description": "Number of files analyzed within declared scope"
    },
    "cosmetic_changes": {
      "type": "integer",
      "description": "Number of COSMETIC changes applied"
    },
    "additive_changes": {
      "type": "integer",
      "description": "Number of ADDITIVE changes applied"
    },
    "breaking_halted": {
      "type": "integer",
      "description": "Number of BREAKING changes identified and halted — not applied"
    },
    "diff_path": {
      "type": "string",
      "description": "Path to before/after diff document in .wabblespec/receipts/"
    }
  }
}
