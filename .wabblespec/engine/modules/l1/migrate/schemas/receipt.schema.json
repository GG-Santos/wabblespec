{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Migrate Receipt",
  "description": "Extension fields for migrate-receipt.json in .wabblespec/state/receipts/",
  "type": "object",
  "required": ["change_ref", "migration_type", "script_generated", "phase1_wave", "phase2_gate_confirmed"],
  "properties": {
    "change_ref": {
      "type": "string",
      "description": "Specify delta ID that triggered this migration"
    },
    "migration_type": {
      "type": "string",
      "enum": ["two-phase", "single-phase"]
    },
    "single_phase_justification": {
      "type": "string",
      "description": "Required when migration_type is single-phase"
    },
    "script_generated": {
      "type": "boolean",
      "description": "Whether an automated migration script was produced"
    },
    "script_path": {
      "type": "string",
      "description": "Path to migration script if script_generated is true"
    },
    "phase1_wave": {
      "type": "string",
      "description": "Wave ID for Phase 1 additive changes"
    },
    "phase2_wave": {
      "type": "string",
      "description": "Wave ID for Phase 2 removal — set after gate confirmed"
    },
    "phase2_gate_confirmed": {
      "type": "boolean",
      "description": "Human confirmation that all consumers have migrated — required before Phase 2 executes"
    },
    "migration_plan_path": {
      "type": "string",
      "description": "Path to the migration plan document"
    }
  }
}
