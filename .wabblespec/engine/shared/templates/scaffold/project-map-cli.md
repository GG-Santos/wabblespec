# Project Map

**Project:** ___ UNDECLARED  
**Platform:** CLI  
**Language:** ___ UNDECLARED (Node.js / Python / Go / Rust)  
**Scaffolded:** ___ UNDECLARED  
**explored_at:** ___ UNDECLARED  
**build_target:** cli  
**freshness_state:** FRESH  
**valid_until:** ___ UNDECLARED (24h from explored_at)  
**WabbleSpec version:** ___ UNDECLARED  

---

## Entry Points

_Fill in the language-appropriate entry point:_

- **Node.js:** `src/index.ts` or `bin/{cli-name}.ts`
- **Python:** `src/{package}/__main__.py` or `src/{package}/cli.py`
- **Go:** `cmd/{cli-name}/main.go`
- **Rust:** `src/main.rs`

_Update to match actual entry after Scaffold generates structure._

## Tech Stack

| Area | Technology | Version | Confidence |
|---|---|---|---|
| runtime | ___ UNDECLARED (Node/Python/Go/Rust) | ___ UNDECLARED | exact |
| language | ___ UNDECLARED | ___ UNDECLARED | exact |
| cli-framework | ___ UNDECLARED (Commander.js/Click/Cobra/Clap) | ___ UNDECLARED | exact |
| test | ___ UNDECLARED (Jest/pytest/go test/cargo test) | ___ UNDECLARED | exact |
| distribution | ___ UNDECLARED (npm/PyPI/Homebrew/cargo/binary release) | n/a | inferred |
| lint | ___ UNDECLARED | ___ UNDECLARED | exact |
| ci | ___ UNDECLARED | n/a | inferred |
| telemetry | ___ UNDECLARED (opt-in only — declared in build-toolchain.md) | n/a | assumed |

_Confidence: `exact` = read from lock file, `inferred` = read from manifest without lock, `assumed` = no manifest evidence._

## Risk Files

| Path | Risk reason | Churn count | Note |
|---|---|---|---|
| `src/commands/` (or equivalent) | cross-cutting | 0 | All command definitions — user-facing contract |
| `src/index.ts` (or entry point) | cross-cutting | 0 | CLI entry — all commands registered here |
| `src/config/` (or equivalent) | cross-cutting | 0 | Config loading — affects all commands |

_Populate from git history after first N commits._

## Impact Slices

### entry-points
**Reason:** CLI entry and command registration — all command changes flow through here.

- `src/index.ts` (update to actual entry)
- `src/commands/` (all command files)
- `bin/` (executable entry points)

### api-surface
**Reason:** Public command interface — the user-facing CLI contract.

- `src/commands/` (all command definitions with flags and arguments)
- `src/types/` (shared TypeScript/Python types — update)
- README.md (command usage documentation — reflects public interface)

**Test paths:**
- `tests/commands/` or `src/commands/__tests__/` (update)

### test-coverage
**Reason:** Test file locations — CLI needs both unit tests and integration tests (subprocess invocation).

- `tests/` (root test directory)
- `tests/integration/` (subprocess invocation tests — test actual CLI output)
- `src/**/*.test.ts` (unit tests — update to match language convention)

### risk
**Reason:** Files from Risk Files table above.

- `src/commands/`
- `src/index.ts`
- `src/config/`

### conventions
**Reason:** Command naming, flag conventions, output format patterns.

- _See Conventions section below_

## Spec Artifacts

| Path | Artifact type | Status |
|---|---|---|
| `.wabblespec/receipts/scaffold-receipt-{timestamp}.json` | scaffold-receipt | LOCKED |

## Conventions

- Command naming: lowercase hyphen-separated (e.g. `generate-report`, not `generateReport`)
- Flag naming: lowercase hyphen-separated (e.g. `--output-format`, not `--outputFormat`)
- Exit codes: 0 = success, 1 = general error, 2 = usage error (declared in performance-budgets.md)
- Output format: human-readable by default; `--json` flag for machine-readable output
- Config file: `~/.config/{cli-name}/config.{ext}` (XDG compliant — update if different)
- Error output: stderr; normal output: stdout (allows piping)
- ___ UNDECLARED — add project-specific conventions after first Explore run

## Git State

- **Active branch:** ___ UNDECLARED
- **Recent changes:** (none — fresh scaffold)
- **Uncommitted count:** ___ UNDECLARED

## Gaps

- Command list: not yet determined (unknown until subcommands are implemented)
- Config schema: not yet defined
- Distribution format: inferred from build-toolchain.md — confirm with first release
- Telemetry: assumed absent — confirm opt-in/opt-out policy from build-toolchain.md declaration
- Cross-platform compatibility: CI matrix planned (Linux/macOS/Windows) — not verified until first cross-platform build
- Shell completion scripts: not yet generated
- Test coverage baseline: 0% (fresh scaffold)

---

*Generated by Scaffold from `.wabblespec/engine/shared/templates/scaffold/project-map-cli.md`. Replace all `___ UNDECLARED` with actual values. Explore updates this file after generation.*
</content>
