# Project Map

**Project:** ___ UNDECLARED  
**Platform:** Mobile  
**Language:** ___ UNDECLARED (TypeScript/Dart/Swift+Kotlin)  
**Scaffolded:** ___ UNDECLARED  
**explored_at:** ___ UNDECLARED  
**build_target:** mobile  
**freshness_state:** FRESH  
**valid_until:** ___ UNDECLARED (24h from explored_at)  
**WabbleSpec version:** ___ UNDECLARED  

---

## Entry Points

_Fill in the framework-appropriate entry point:_

- **React Native:** `App.tsx` or `src/App.tsx`
- **Flutter:** `lib/main.dart`
- **Swift (iOS native):** `{AppName}/AppDelegate.swift` or `{AppName}App.swift` (SwiftUI)
- **Kotlin (Android native):** `app/src/main/java/{package}/MainActivity.kt`

_Update to match actual entry after Scaffold generates structure._

## Tech Stack

| Area | Technology | Version | Confidence |
|---|---|---|---|
| framework | ___ UNDECLARED (React Native / Flutter / Native iOS / Native Android) | ___ UNDECLARED | exact |
| language | ___ UNDECLARED (TypeScript / Dart / Swift / Kotlin) | ___ UNDECLARED | exact |
| state | ___ UNDECLARED (Zustand / Provider / Redux / TCA — update) | ___ UNDECLARED | inferred |
| navigation | ___ UNDECLARED (React Navigation / GoRouter / UIKit / Jetpack Nav) | ___ UNDECLARED | inferred |
| test | ___ UNDECLARED (Jest/RN Testing Library / Flutter test / XCTest / JUnit) | ___ UNDECLARED | exact |
| ci | ___ UNDECLARED | n/a | inferred |
| crash-reporting | ___ UNDECLARED (Firebase Crashlytics / Sentry — declared in build-toolchain.md) | n/a | assumed |
| ota | ___ UNDECLARED (CodePush / Expo Updates — if applicable) | n/a | assumed |

## Risk Files

| Path | Risk reason | Churn count | Note |
|---|---|---|---|
| `App.tsx` (or framework entry) | cross-cutting | 0 | Root component / app bootstrap — all screens flow from here |
| `src/navigation/` (or equivalent) | cross-cutting | 0 | Navigation tree — affects all screen routing |
| `android/` and `ios/` native dirs | cross-cutting | 0 | Native config — signing, permissions, capabilities |
| `src/api/` (or equivalent) | no-tests | 0 | API client layer — requires dedicated tests |

## Impact Slices

### entry-points
**Reason:** App initialization and root navigation — structural changes start here.

- `App.tsx` (update to actual entry)
- `src/navigation/` (root navigator)
- `src/screens/` or `src/pages/` (screen directory)

### api-surface
**Reason:** API client and data layer — how the app communicates with backends.

- `src/api/` (API client — update)
- `src/services/` (business logic layer — update)
- `src/types/` (shared types — update)

**Test paths:**
- `src/api/__tests__/` or `__tests__/api/` (update)

### test-coverage
**Reason:** Test file locations — mobile needs unit, component, and E2E (Detox/Maestro).

- `__tests__/` (Jest unit tests)
- `src/**/*.test.ts` (colocated unit tests)
- `e2e/` (Detox / Maestro E2E tests — update if used)

### risk
**Reason:** Files from Risk Files table above.

- `App.tsx`
- `src/navigation/`
- `android/`
- `ios/`

### conventions
**Reason:** Screen naming, component structure, and platform-specific patterns.

- _See Conventions section below_

## Spec Artifacts

| Path | Artifact type | Status |
|---|---|---|
| `.wabblespec/state/receipts/scaffold-receipt-{timestamp}.json` | scaffold-receipt | LOCKED |

## Conventions

- Screen components: PascalCase with `Screen` suffix (e.g. `HomeScreen.tsx`)
- Shared components: PascalCase, in `src/components/` (e.g. `PrimaryButton.tsx`)
- Navigation routes: typed with TypeScript — route params schema in `src/navigation/types.ts`
- Platform-specific files: `.ios.ts` / `.android.ts` suffix convention
- Asset naming: lowercase hyphen-separated (e.g. `icon-home.png`)
- ___ UNDECLARED — add project-specific conventions after first Explore run

## Git State

- **Active branch:** ___ UNDECLARED
- **Recent changes:** (none — fresh scaffold)
- **Uncommitted count:** ___ UNDECLARED

## Gaps

- Screen list: not yet determined (unknown until navigation tree is implemented)
- Deep linking scheme: not configured
- Push notification configuration: unknown
- Code signing: declared in build-toolchain.md — certificates not yet provisioned
- OTA update policy: assumed from build-toolchain.md — verify before first release
- Crash reporting: assumed from build-toolchain.md — not yet integrated
- Permissions manifest: not yet reviewed (iOS Info.plist / Android Manifest.xml)
- Test coverage baseline: 0% (fresh scaffold)

---

*Generated by Scaffold from `.wabblespec/engine/shared/templates/scaffold/project-map-mobile.md`. Replace all `___ UNDECLARED` with actual values. Explore updates this file after generation.*
