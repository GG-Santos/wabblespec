# Project Map

**Project:** ___ UNDECLARED  
**Platform:** Desktop  
**Language:** TypeScript (Electron) / Rust (Tauri)  
**Scaffolded:** ___ UNDECLARED  
**explored_at:** ___ UNDECLARED  
**build_target:** desktop  
**freshness_state:** FRESH  
**valid_until:** ___ UNDECLARED (24h from explored_at)  
**WabbleSpec version:** ___ UNDECLARED  

---

## Entry Points

_Fill in the framework-appropriate entry points (desktop apps have both main/native and renderer/frontend processes):_

**Electron:**
- Main process: `src/main/index.ts`
- Renderer process: `src/renderer/index.tsx` (or `src/renderer/main.tsx`)
- Preload: `src/preload/index.ts`

**Tauri:**
- Rust backend: `src-tauri/src/main.rs`
- Frontend: `src/main.ts` or `src/App.tsx`
- Tauri config: `src-tauri/tauri.conf.json`

_Update to match actual structure after Scaffold generates._

## Tech Stack

| Area | Technology | Version | Confidence |
|---|---|---|---|
| framework | ___ UNDECLARED (Electron / Tauri) | ___ UNDECLARED | exact |
| frontend-language | TypeScript | ___ UNDECLARED | exact |
| backend-language | ___ UNDECLARED (Node.js / Rust) | ___ UNDECLARED | exact |
| frontend-framework | ___ UNDECLARED (React / Vue / Svelte) | ___ UNDECLARED | exact |
| bundler | ___ UNDECLARED (Vite / webpack) | ___ UNDECLARED | exact |
| test | ___ UNDECLARED (Vitest + Playwright / cargo test) | ___ UNDECLARED | exact |
| updater | ___ UNDECLARED (electron-updater / Tauri updater — declared in build-toolchain.md) | ___ UNDECLARED | inferred |
| signing | ___ UNDECLARED (Gatekeeper/SmartScreen — declared in build-toolchain.md) | n/a | inferred |
| ci | ___ UNDECLARED | n/a | inferred |

## Risk Files

| Path | Risk reason | Churn count | Note |
|---|---|---|---|
| `src/main/index.ts` (or Tauri main.rs) | cross-cutting | 0 | Main process entry — IPC, window management, native APIs |
| `src/preload/index.ts` (Electron only) | cross-cutting | 0 | IPC bridge — security boundary between main and renderer |
| `src-tauri/tauri.conf.json` (Tauri only) | cross-cutting | 0 | Permissions, window config, update config |
| `src/renderer/` (or frontend root) | cross-cutting | 0 | Renderer process root — all UI flows through here |

## Impact Slices

### entry-points
**Reason:** Main process and renderer bootstraps — all IPC and UI changes start here.

**Electron:**
- `src/main/index.ts`
- `src/renderer/index.tsx`
- `src/preload/index.ts`

**Tauri:**
- `src-tauri/src/main.rs`
- `src/main.ts`
- `src-tauri/tauri.conf.json`

### api-surface
**Reason:** IPC channels and native API surface — the contract between main and renderer.

- `src/preload/index.ts` (Electron IPC channels — public API of the native layer)
- `src-tauri/src/commands/` (Tauri commands — update if used)
- `src/shared/types/` (shared IPC types — update to actual path)

**Test paths:**
- `tests/ipc/` or `src/__tests__/ipc/` (IPC integration tests — update)

### test-coverage
**Reason:** Unit + integration + E2E (headed/headless) test locations.

- `tests/` (root test directory)
- `src/**/*.test.ts` (unit tests)
- `tests/e2e/` (Playwright headless desktop E2E — update)

### risk
**Reason:** Files from Risk Files table above.

- `src/main/index.ts` (or `src-tauri/src/main.rs`)
- `src/preload/index.ts`
- `src-tauri/tauri.conf.json`

### conventions
**Reason:** IPC naming, window management, and process boundary patterns.

- _See Conventions section below_

## Spec Artifacts

| Path | Artifact type | Status |
|---|---|---|
| `.wabblespec/state/receipts/scaffold-receipt-{timestamp}.json` | scaffold-receipt | LOCKED |

## Conventions

- IPC channel names: namespace-prefixed (`app:`, `file:`, `window:` — e.g. `file:open-dialog`)
- All renderer→native calls go through preload (no direct Node/native access from renderer — security boundary)
- Window state persistence: `electron-store` or `localStorage` (declare in `src/main/state/`)
- Auto-updater: configured in `src/main/updater.ts` (Electron) or `src-tauri/tauri.conf.json` (Tauri)
- Code signing: macOS Gatekeeper / Windows SmartScreen — certificates declared in build-toolchain.md
- ___ UNDECLARED — add project-specific conventions after first Explore run

## Git State

- **Active branch:** ___ UNDECLARED
- **Recent changes:** (none — fresh scaffold)
- **Uncommitted count:** ___ UNDECLARED

## Gaps

- IPC channel contract: not yet defined (unknown until first main-renderer communication is implemented)
- Native API permissions: not yet declared (`tauri.conf.json` allowlist or Electron API usage)
- Auto-updater server URL: not configured
- Code signing certificates: declared in build-toolchain.md — not yet provisioned
- Deep link URL scheme: not configured
- Platform-specific build targets (macOS arm64/x64, Windows x64, Linux): not yet verified
- Test coverage baseline: 0% (fresh scaffold)

---

*Generated by Scaffold from `.wabblespec/engine/shared/templates/scaffold/project-map-desktop.md`. Replace all `___ UNDECLARED` with actual values. Explore updates this file after generation.*
