# Project Map

**Project:** ___ UNDECLARED  
**Platform:** AI-Agent  
**Language:** Python / TypeScript  
**Scaffolded:** ___ UNDECLARED  
**explored_at:** ___ UNDECLARED  
**build_target:** ai-agent  
**freshness_state:** FRESH  
**valid_until:** ___ UNDECLARED (24h from explored_at)  
**WabbleSpec version:** ___ UNDECLARED  

---

## Entry Points

- `src/agent.py` (or `src/agent.ts`) — main agent loop
- `src/tools/` — tool definitions
- `src/prompts/` — system prompt and prompt templates
- `evals/` — eval harness and fixtures (required by L4/AI gateway)

## Tech Stack

| Area | Technology | Version | Confidence |
|---|---|---|---|
| language | ___ UNDECLARED (Python / TypeScript) | ___ UNDECLARED | exact |
| llm-framework | ___ UNDECLARED (LangChain / LlamaIndex / Anthropic SDK / OpenAI SDK) | ___ UNDECLARED | exact |
| primary-model | ___ UNDECLARED (pinned — NOT latest) | ___ UNDECLARED | exact |
| fallback-model | ___ UNDECLARED (pinned — NOT latest) | ___ UNDECLARED | exact |
| eval-harness | ___ UNDECLARED (pytest / promptfoo / LangSmith) | ___ UNDECLARED | exact |
| vector-store | ___ UNDECLARED (if used) | ___ UNDECLARED | inferred |
| test | ___ UNDECLARED | ___ UNDECLARED | exact |
| ci | ___ UNDECLARED | n/a | inferred |
| cost-tracking | ___ UNDECLARED (declared in build-toolchain.md) | n/a | inferred |

_Primary and fallback models must be pinned — `latest` is a BLOCK per L4/AI gateway._

## Risk Files

| Path | Risk reason | Churn count | Note |
|---|---|---|---|
| `src/prompts/system.md` (or equivalent) | high-churn | 0 | System prompt — changes affect all agent behavior; eval suite must be run on every change |
| `src/agent.py` (or equivalent) | cross-cutting | 0 | Agent loop — iteration ceiling and tool-call logic lives here |
| `src/tools/` | cross-cutting | 0 | Tool definitions — each tool change affects agent capability surface |
| `evals/fixtures/` | no-tests | 0 | Eval fixtures — must not be changed without re-running full eval suite |

## Impact Slices

### entry-points
**Reason:** Agent bootstrap and main loop — all agent runs pass through here.

- `src/agent.py` (or `src/agent.ts`)
- `src/prompts/system.md`
- `src/tools/`

### api-surface
**Reason:** Public interface — how callers invoke the agent and what it returns.

- `src/agent.py` — agent invocation signature
- `src/types/` or `src/models/` — input/output type declarations
- `src/tools/` — tool schemas (visible to the LLM)

**Test paths:**
- `evals/` (eval suite — primary correctness signal for AI agents)
- `tests/` (unit tests for non-LLM components)

### test-coverage
**Reason:** Eval fixtures + unit tests — two distinct coverage dimensions for AI agents.

- `evals/fixtures/` (held-out eval cases — do not modify without re-running)
- `evals/` (eval harness)
- `tests/` (unit tests for tool implementations, parsers, non-LLM logic)

### risk
**Reason:** Files from Risk Files table above.

- `src/prompts/system.md`
- `src/agent.py`
- `src/tools/`
- `evals/fixtures/`

### conventions
**Reason:** Tool naming, prompt versioning, and eval fixture patterns.

- _See Conventions section below_

## Spec Artifacts

| Path | Artifact type | Status |
|---|---|---|
| `.wabblespec/receipts/scaffold-receipt-{timestamp}.json` | scaffold-receipt | LOCKED |

## Conventions

- Tool names: lowercase underscore-separated (e.g. `search_web`, `read_file`) — must match tool schema `name` field exactly
- System prompt: versioned with header comment `# System Prompt v{N}` — increment on every substantive change
- Eval fixtures: JSON files, one case per file, named `{capability}-{case-description}.json`
- Model references: always pinned version string — never `latest`, `default`, or alias
- Irreversible tool calls: must require Attestation before execution (per L7/deploy Attestation policy)
- Iteration ceiling: declared in `src/agent.py` as a constant — never open-ended loop
- ___ UNDECLARED — add project-specific conventions after first Explore run

## Git State

- **Active branch:** ___ UNDECLARED
- **Recent changes:** (none — fresh scaffold)
- **Uncommitted count:** ___ UNDECLARED

## Gaps

- Tool list: not yet finalized (unknown until first feature wave implements tools)
- Eval pass rate baseline: 0% (no evals yet — required before first version bump per L4/AI gateway)
- Prompt injection test suite: not yet implemented (required in build-toolchain.md)
- Cost ceiling per operation: declared in performance-budgets.md — not yet verified against real calls
- Model version drift check: CI gate declared in build-toolchain.md — not yet active
- Memory/context persistence strategy: not determined (stateless vs. conversation memory)
- Human-in-the-loop gates: not yet defined for irreversible operations
- Token budget per operation: declared in performance-budgets.md — not validated until first real run

---

*Generated by Scaffold from `.wabblespec/engine/shared/templates/scaffold/project-map-ai-agent.md`. Replace all `___ UNDECLARED` with actual values. Explore updates this file after generation. L4/AI gateway activates on every wave for this platform.*
